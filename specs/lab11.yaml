---
assignment: lab11
folder: projtech

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - - proto-chat.cpp
    - rm -f *.o proto-chat
    - make proto-chat
  - - proto-multiplayer.cpp
    - rm -f *.o proto-multiplayer
    - make proto-multiplayer
