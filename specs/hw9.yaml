---
assignment: hw9

compilers:
  cpp: &cpp
    g++ --std=c++11 $@ -o $@.exec

files:
  - reverse.cpp: *cpp
  - Doxyfile: cat $@ ../../../Doxyfile | doxygen -

tests:
  reverse.cpp:
    - $@.exec

options:
  Doxyfile:
    truncate_contents: 100  # bytes
