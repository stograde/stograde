---
assignment: hw9

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - [ reverse.cpp, *cpp ]
  - - Doxyfile
    - cat $@ ../../../Doxyfile | doxygen -
    - hide_contents: true

tests:
  - [ reverse.cpp, $@.exec ]
