---
assignment: lab9
folder: graphics

compilers:
  - &cpp 'g++ --std=c++11 $@ -o $@.exec'

files:
  - - proto-draw.cpp
    - rm -f *.o proto-draw
    - make proto-draw

  - [ colorButtons.txt, null ]

  - [ toolButtons.txt, null, optional: true ]
